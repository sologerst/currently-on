NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# TMDb (server-only — never NEXT_PUBLIC_)
TMDB_READ_ACCESS_TOKEN=
TMDB_API_KEY=

# Auth (Dashboard): enable Email provider with password + magic link/OTP.
# Site URL + Redirect URLs must include /auth/confirm (and preview domains).
# Recommended: min password length 8+, leaked password protection on Pro.
